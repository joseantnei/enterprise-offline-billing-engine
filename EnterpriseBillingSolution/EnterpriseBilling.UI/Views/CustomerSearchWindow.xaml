<Window x:Class="EnterpriseBilling.UI.Views.CustomerSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🔍 Buscar Cliente" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#F8FAFC">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Filtrar por Nombre o Identificación (Tax ID):" FontWeight="SemiBold" Foreground="#334155" Margin="0,0,0,5"/>
            <TextBox x:Name="txtSearch" Height="35" VerticalContentAlignment="Center" Padding="5,0" TextChanged="TxtSearch_TextChanged"/>
        </StackPanel>

        <DataGrid x:Name="dgCustomers" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True" RowHeight="30" SelectionMode="Single" MouseDoubleClick="DgCustomers_MouseDoubleClick" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nombre / Razón Social" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="120"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Grid.Row="2" Content="Seleccionar Cliente" Height="40" Margin="0,15,0,0" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0" Click="BtnSelect_Click"/>
    </Grid>
</Window>