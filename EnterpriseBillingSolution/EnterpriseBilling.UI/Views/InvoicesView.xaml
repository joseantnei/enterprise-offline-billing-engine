<UserControl x:Class="EnterpriseBilling.UI.Views.InvoicesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:EnterpriseBilling.UI.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,10,0">
                    <TextBlock Text="Client: " FontWeight="Bold" Foreground="#475569" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding CustomerName, Mode=TwoWay}" IsReadOnly="True" Height="35" VerticalContentAlignment="Center" Background="#F1F5F9" Padding="5,0"/>
                    <Button Grid.Column="1" Content=" 🔍 Buscar " Command="{Binding OpenCustomerSearchCommand}" Height="35" Width="80" Margin="5,0,0,0" Background="#64748B" Foreground="White" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,10,0">
                    <TextBlock Text="Search" FontWeight="Bold" Foreground="#475569" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding BarcodeSearch, UpdateSourceTrigger=PropertyChanged}" Height="35" VerticalContentAlignment="Center" Padding="5,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchProductCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                </StackPanel>

                <Button Grid.Column="2" Content="New" Command="{Binding SearchProductCommand}" Height="35" VerticalAlignment="Bottom" Background="#3B82F6" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </Grid>
        </Border>

        <DataGrid Grid.Row="1" ItemsSource="{Binding InvoiceItems}" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" RowHeight="35" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#E2E8F0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="BarCode" Binding="{Binding Barcode}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Quantify" Binding="{Binding Quantity, UpdateSourceTrigger=PropertyChanged}" Width="70"/>
                <DataGridTextColumn Header="Price Unit." Binding="{Binding UnitPrice, StringFormat=C2}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat=C2}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="2" Margin="0,15,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="🔒 Save and Issue Invoice" Command="{Binding SaveInvoiceCommand}" Height="50" HorizontalAlignment="Left" Width="250" Background="#10B981" Foreground="White" FontSize="16" FontWeight="Bold" BorderThickness="0"/>

            <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="20,10" MinWidth="250">
                <StackPanel>
                    <Grid Margin="0,2">
                        <TextBlock Text="Subtotal:" Foreground="#64748B"/>
                        <TextBlock Text="{Binding SubTotal, StringFormat=C2}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>
                    <Grid Margin="0,2">
                        <TextBlock Text="Total Taxes:" Foreground="#64748B"/>
                        <TextBlock Text="{Binding TotalTaxes, StringFormat=C2}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                    </Grid>
                    <Separator Margin="0,5" Background="#E2E8F0"/>
                    <Grid Margin="0,2">
                        <TextBlock Text="TOTAL PAY:" FontSize="16" FontWeight="Bold" Foreground="#1E293B"/>
                        <TextBlock Text="{Binding TotalPay, StringFormat=C2}" HorizontalAlignment="Right" FontSize="16" FontWeight="Bold" Foreground="#3B82F6"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
